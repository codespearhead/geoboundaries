/*
This has been generated by the overpass-turbo wizard.
The original search was:
“("type"="boundary" or "place"="suburb") in "Americana, SP"”
*/
[out:json][timeout:25];
// fetch area “Americana, SP” to search in
{{geocodeArea:Americana, SP}}->.searchArea;
// gather results
(
  // query part for: “type=boundary”
  node["type"="boundary"](area.searchArea);
  way["type"="boundary"](area.searchArea);
  relation["type"="boundary"](area.searchArea);
  // query part for: “place=suburb”
  node["place"="suburb"](area.searchArea);
  way["place"="suburb"](area.searchArea);
  relation["place"="suburb"](area.searchArea);
);
// print results
out body;
>;
out skel qt;